# 빌드 임시 산출물만 제외 — .exe + DLL은 git에 포함 (다른 PC에서 컴파일러 없이 실행용)
build/*.obj
build/*.o
build/*.txt
build/hello.*
*.o
_dbg_*

# 사운드: 원본(sounds/) + build/sounds/ 둘 다 커밋.
# 커밋된 build/PokemonRed.exe 는 exe 옆 build/sounds/ 를 찾으므로, 컴파일러 없이
# clone 후 exe 만 더블클릭하는 PC 에서도 BGM·효과음이 나오려면 build/sounds/ 도 포함해야 한다.

# python 캐시 (사운드 합성기 tools/synth_audio.py)
__pycache__/
*.pyc

# editor / OS
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
